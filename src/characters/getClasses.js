import { longbowmen } from './longbowmen.js';
import { halberdier } from './halberdier.js';

export default function getClasses() {
  console.log(`${longbowmen} and ${halberdier} getClasses() was called from index.js`);
}